---
alwaysApply: false
---
# TDD Workflow - Test-Driven Development Process

> Workflow для разработки по TDD подходу. См. также @qa_conventions.mdc

## 🎯 Главные правила TDD

**RED → GREEN → REFACTOR. Тест всегда первым. Минимальный код для прохождения.**

---

## 📋 TDD процесс работы над итерацией

### 1. Начало итерации

**Действия:**
- [ ] Прочитай задачи текущей итерации из TASKLIST.md
- [ ] Изучи требования из VISION.md, CONVENTIONS.md, qa_conventions.mdc
- [ ] Обнови статус итерации в TASKLIST.md на `🔄 В работе`
- [ ] Убедись что baseline работает: `make ci`

**Baseline check:**
```bash
make ci  # Все должно быть зеленым
```

### 2. Согласование решения + тестовых сценариев

**Действия:**
- [ ] Предложи архитектурное решение
- [ ] **Определи тестовые сценарии для каждой фичи**
- [ ] Покажи ключевые фрагменты кода и тестов
- [ ] Объясни как решение соответствует KISS
- [ ] **Дождись подтверждения от пользователя**

**Формат согласования:**
```
Предлагаю TDD решение для [задача]:

### Структура кода
- Файл: src/example.py
- Класс: ExampleClass
- Публичные методы: method_a(), method_b()

### Тестовые сценарии (будут написаны ПЕРВЫМИ)
1. test_method_a_success_case() - основной сценарий
2. test_method_a_with_empty_input() - граничный случай
3. test_method_a_raises_on_invalid() - обработка ошибок
4. test_method_b_integration() - интеграция с другими компонентами

### Пример ключевого метода (ПОСЛЕ тестов)
```python
class ExampleClass:
    async def method_a(self, input: str) -> str:
        """Process input and return result."""
        pass
```

### TDD план
1. 🔴 Пишем test_method_a_success_case() (RED)
2. 🟢 Реализуем method_a() минимально (GREEN)
3. ♻️ Рефакторим если нужно (REFACTOR)
4. Повторяем для остальных тестов

Соответствие принципам:
✅ KISS - простое решение
✅ Тесты покрывают основной функционал
✅ 1 класс = 1 файл
✅ Избегаем boilerplate тестов

Согласовать?
```

### 3. TDD Реализация (Red-Green-Refactor циклы)

**Для каждого тестового сценария выполняй цикл:**

#### 🔴 RED Phase - Failing Test

**Действия:**
- [ ] Напиши failing test для одной фичи
- [ ] Тест должен проверять ожидаемое поведение
- [ ] Запусти `make test` - тест должен провалиться (RED)

**Шаблон RED:**
```python
# tests/test_example.py

@pytest.mark.asyncio
async def test_method_a_success_case():
    """Test method_a returns processed result on valid input."""
    # Arrange
    instance = ExampleClass()
    test_input = "test data"

    # Act
    result = await instance.method_a(test_input)

    # Assert
    assert result == "processed: test data"
```

**Команды RED:**
```bash
make test  # Должен провалиться (RED) ❌
# Expected: AttributeError или AssertionError
```

**Формат сообщения RED:**
```
🔴 RED: test_method_a_success_case

Написан failing test:
- Тестирует: method_a() с валидным вводом
- Ожидает: "processed: test data"

Результат make test:
❌ FAILED - AttributeError: ExampleClass has no attribute 'method_a'

Переходим к GREEN фазе?
```

#### 🟢 GREEN Phase - Minimal Implementation

**Действия:**
- [ ] Реализуй **минимальный** код для прохождения теста
- [ ] Не добавляй лишний функционал
- [ ] Запусти `make test` - тест должен пройти (GREEN)
- [ ] Убедись что старые тесты не сломались

**Шаблон GREEN:**
```python
# src/example.py

class ExampleClass:
    """Example class with method_a."""

    async def method_a(self, input: str) -> str:
        """
        Process input and return result.

        Args:
            input: Input string to process

        Returns:
            Processed string with prefix
        """
        # Минимальная реализация для прохождения теста
        return f"processed: {input}"
```

**Команды GREEN:**
```bash
make test       # Новый тест должен пройти (GREEN) ✅
make test-cov   # Проверка покрытия
```

**Формат сообщения GREEN:**
```
🟢 GREEN: test_method_a_success_case

Реализован минимальный код:
- Метод method_a() в src/example.py
- Возвращает нужный формат
- Добавлен docstring

Результат make test:
✅ PASSED - test_method_a_success_case

Coverage: 87% (+2%)

Переходим к REFACTOR фазе?
```

#### ♻️ REFACTOR Phase - Improve Code

**Действия:**
- [ ] Улучши код если есть дублирование/сложность
- [ ] Следуй CONVENTIONS.md
- [ ] Запусти `make ci` - все должно проходить
- [ ] Тесты остаются зелеными

**Когда рефакторить:**
- Видишь дублирование кода (DRY)
- Код слишком сложный (KISS)
- Можно улучшить читаемость
- Нарушены соглашения

**Когда НЕ рефакторить:**
- Код уже простой и понятен
- "Улучшения" усложняют код
- Это просто другая реализация

**Команды REFACTOR:**
```bash
make lint       # Проверка стиля
make type-check # Проверка типов
make test       # Тесты остаются зелеными ✅
make ci         # Полная проверка
```

**Формат сообщения REFACTOR:**
```
♻️ REFACTOR: Улучшен ExampleClass

Изменения:
- Вынес общую логику в _helper()
- Улучшены имена переменных
- Добавлены type hints

Результат make ci:
✅ lint - чист
✅ type-check - 0 ошибок
✅ test - все тесты проходят
✅ Coverage: 87%

Следующий тест или готово?
```

---

### 4. Итерация по всем тестовым сценариям

**Для каждого следующего сценария повторяй:**

```
Сценарий 1: test_method_a_success_case
🔴 RED → 🟢 GREEN → ♻️ REFACTOR
   ↓
Сценарий 2: test_method_a_with_empty_input
🔴 RED → 🟢 GREEN → ♻️ REFACTOR
   ↓
Сценарий 3: test_method_a_raises_on_invalid
🔴 RED → 🟢 GREEN → ♻️ REFACTOR
   ↓
Все тесты зеленые ✅
```

**После каждого цикла:**
- [ ] Отметь чекбокс в списке тестовых сценариев
- [ ] Убедись что `make ci` проходит
- [ ] Дождись подтверждения на следующий сценарий

---

### 5. Подтверждение реализации

**После завершения всех TDD циклов:**
- [ ] Покажи что реализовано
- [ ] Покажи результаты `make ci`
- [ ] Укажи метрики (coverage, тесты)
- [ ] **Дождись подтверждения от пользователя**

**Формат:**
```
Реализовано (TDD):
✅ src/example.py - класс ExampleClass
✅ tests/test_example.py - 4 теста

Все TDD циклы пройдены:
✅ test_method_a_success_case (RED → GREEN → REFACTOR)
✅ test_method_a_with_empty_input (RED → GREEN → REFACTOR)
✅ test_method_a_raises_on_invalid (RED → GREEN → REFACTOR)
✅ test_method_b_integration (RED → GREEN → REFACTOR)

Результаты качества:
✅ make ci - полностью зеленый
✅ Coverage: 85% → 89%
✅ Все тесты: 24 passed
✅ Mypy: 0 errors
✅ Ruff: 0 issues

Готово к финальной проверке?
```

---

### 6. Завершение итерации

**После подтверждения:**
- [ ] Обнови прогресс в TASKLIST.md:
  - Статус на `✅ Завершено`
  - Дату завершения
  - Метрики в примечаниях
- [ ] Предложи текст коммита с TDD метриками
- [ ] **Дождись подтверждения перехода к следующей итерации**

**Формат коммита:**
```
feat: [краткое описание итерации] (TDD)

Реализовано:
- [класс/модуль] с полным test coverage
- [метод 1] с edge cases
- [метод 2] с error handling

Тесты (TDD approach):
- tests/test_example.py: 4 новых теста
- Coverage: 85% → 89%
- All tests: 24 passed

Refs: TASKLIST.md - Итерация N
```

---

## 🎨 TDD Шаблоны

### Шаблон RED фазы

```python
# 🔴 RED: Пишем failing test

def test_feature_scenario():
    """Test feature behavior in specific scenario."""
    # Arrange
    setup = prepare_test_data()

    # Act
    result = feature(setup)

    # Assert
    assert result == expected_value
```

**Checklist RED:**
- [ ] Тест проверяет одну фичу
- [ ] Понятное имя теста
- [ ] AAA структура (Arrange-Act-Assert)
- [ ] `make test` проваливается (RED) ❌

### Шаблон GREEN фазы

```python
# 🟢 GREEN: Минимальный код для прохождения

def feature(input_data):
    """
    Feature description.

    Args:
        input_data: Input description

    Returns:
        Result description
    """
    # Простейшая реализация
    return process(input_data)
```

**Checklist GREEN:**
- [ ] Код минимальный (не больше нужного)
- [ ] Есть docstring
- [ ] Есть type hints
- [ ] `make test` проходит (GREEN) ✅
- [ ] Старые тесты не сломались

### Шаблон REFACTOR фазы

**Checklist REFACTOR:**
- [ ] Убрано дублирование (DRY)
- [ ] Код упрощен (KISS)
- [ ] Следует CONVENTIONS.md
- [ ] `make ci` полностью зеленый ✅
- [ ] Тесты остаются зелеными

---

## ⚠️ TDD правила

### Никогда не делай

❌ **Не пиши код до теста** (только GREEN после RED)
❌ **Не пиши больше кода** чем нужно для прохождения теста
❌ **Не пропускай RED фазу** ("тест должен быть красным")
❌ **Не игнорируй REFACTOR** если код дублируется или сложен
❌ **Не переходи к следующему тесту** пока текущий не GREEN
❌ **Не пиши boilerplate тесты** (см. qa_conventions.mdc)

### Всегда делай

✅ **Начинай с теста** (RED фаза)
✅ **Минимальный код** для прохождения (GREEN фаза)
✅ **Рефактори при необходимости** (REFACTOR фаза)
✅ **Запускай `make test`** после каждой фазы
✅ **Проверяй `make ci`** после REFACTOR
✅ **Жди подтверждения** перед следующим циклом

---

## 🔄 Пример полного TDD цикла

```
1. Читаешь: Итерация N из TASKLIST.md
   ↓
2. Предлагаешь: Решение + тестовые сценарии
   ↓
3. Ждешь: Подтверждение
   ↓
4. TDD Цикл для Сценария 1:

   4.1. 🔴 RED:
        - Пишешь failing test
        - make test (RED ❌)
        - Показываешь результат
        - Ждешь: Подтверждение

   4.2. 🟢 GREEN:
        - Пишешь минимальный код
        - make test (GREEN ✅)
        - Показываешь результат
        - Ждешь: Подтверждение

   4.3. ♻️ REFACTOR (если нужно):
        - Улучшаешь код
        - make ci (GREEN ✅)
        - Показываешь результат
        - Ждешь: Подтверждение
   ↓
5. Повторяешь: Цикл для Сценариев 2, 3, ...
   ↓
6. Сообщаешь: Все готово, показываешь метрики
   ↓
7. Обновляешь: TASKLIST.md
   ↓
8. Предлагаешь: Текст коммита с TDD метриками
   ↓
9. Ждешь: Подтверждение на следующую итерацию
```

---

## 📝 TDD Чек-лист перед началом

- [ ] Baseline зафиксирован (`make ci` зеленый)
- [ ] Определены тестовые сценарии
- [ ] Понятен ожидаемый результат каждого теста
- [ ] План согласован с пользователем
- [ ] Готов писать тесты ПЕРВЫМИ

---

## 📝 TDD Чек-лист для каждой фазы

### RED Phase ✅
- [ ] Тест написан для одной фичи
- [ ] Тест падает с понятной ошибкой
- [ ] `make test` показывает RED ❌
- [ ] Получено подтверждение на GREEN

### GREEN Phase ✅
- [ ] Код минимальный
- [ ] Тест проходит
- [ ] `make test` показывает GREEN ✅
- [ ] Старые тесты не сломались
- [ ] Получено подтверждение на REFACTOR

### REFACTOR Phase ✅
- [ ] Код улучшен (если было что улучшать)
- [ ] Следует CONVENTIONS.md
- [ ] `make ci` полностью зеленый ✅
- [ ] Получено подтверждение на следующий тест

---

## 📊 TDD Метрики успеха

Отслеживай при каждой итерации:

### Test Metrics
- [ ] Тестов написано: [количество]
- [ ] Все тесты проходят: ✅
- [ ] Coverage: [до]% → [после]%

### Code Quality
- [ ] Ruff: 0 issues
- [ ] Mypy: 0 errors
- [ ] Cyclomatic complexity: [значение]

### TDD Process
- [ ] RED → GREEN → REFACTOR циклов: [количество]
- [ ] Тестов до кода: 100%
- [ ] Минимальная реализация: ✅

---

## 🎯 Цель TDD Workflow

**Разработка качественного кода с полным test coverage через дисциплинированный подход: тест всегда первым, минимальный код для прохождения, рефакторинг с safety net.**

---

## 💡 TDD Best Practices

### Размер шагов
- **Маленькие шаги** лучше больших
- Один тест = одна фича
- Лучше 5 маленьких циклов чем 1 большой

### Порядок тестов
1. **Happy path** (основной сценарий)
2. **Edge cases** (граничные случаи)
3. **Error handling** (обработка ошибок)
4. **Integration** (интеграция с другими компонентами)

### Минимализм в GREEN
```python
# ❌ Плохо: слишком много кода
def process(data):
    # Предугадываем будущие требования
    if validate(data) and check_limits(data) and ...:
        # 50 строк кода

# ✅ Хорошо: минимум для прохождения
def process(data):
    return transform(data)
```

### Рефакторинг с умом
- Не рефактори если код простой
- Рефактори если видишь дублирование
- Рефактори если код сложный для понимания
- Не рефактори "для красоты"

---

> **Помни:** RED → GREEN → REFACTOR. Дисциплина TDD = качественный код. Тест всегда первым!
