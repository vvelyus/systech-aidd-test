# Dockerfile для Frontend
# Sprint D0: Basic Docker Setup (MVP)

FROM node:20-slim

WORKDIR /app

# Установка pnpm
RUN npm install -g pnpm

# Копирование файлов зависимостей
COPY package.json ./

# Установка зависимостей (без frozen-lockfile для MVP)
RUN pnpm install

# Копирование исходного кода
COPY . .

# Открываем порт для Next.js
EXPOSE 3000

# Запуск в development режиме (MVP подход)
CMD ["pnpm", "dev"]
